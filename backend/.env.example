# =====================================================
# GIDEON AI CHAT STUDIO - ENVIRONMENT CONFIGURATION
# =====================================================
# SECURITY NOTE: Generate secure random keys for production!
# Run: python3 -c "import secrets; print(secrets.token_hex(32))"

# =====================================================
# SECURITY CONFIGURATION (REQUIRED)
# =====================================================
# JWT Secret Key - CHANGE THIS IN PRODUCTION!
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-production-secret-key-change-this-in-production-2024

# API Key Encryption Key (32-byte base64 encoded)
# Generate with: python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
ENCRYPTION_KEY=your-32-byte-base64-encryption-key-for-api-keys

# =====================================================
# AUTHENTICATION CONFIGURATION
# =====================================================
ACCESS_TOKEN_EXPIRE_MINUTES=60

# =====================================================
# DATABASE CONFIGURATION
# =====================================================
DATABASE_URL=postgresql://gideon:gideon_password_2024@postgres:5432/gideon_db

# =====================================================
# RATE LIMITING (protect against abuse)
# =====================================================
RATE_LIMIT_REQUESTS=100        # Max requests per time window
RATE_LIMIT_WINDOW=900         # Time window in seconds (15 minutes)

# =====================================================
# CORS CONFIGURATION
# =====================================================
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
# For production, specify your domain: https://yourdomain.com

# =====================================================
# SERVER CONFIGURATION
# =====================================================
PORT=8000
DEBUG=False
LOG_LEVEL=INFO

# =====================================================
# VECTOR DATABASE CONFIGURATION
# =====================================================
# ChromaDB (local) - no additional config needed, files stored in ./data/chroma

# Optional: Weaviate (cloud/self-hosted)
WEAVIATE_URL=https://your-weaviate-instance.com

# Optional: Pinecone (cloud)
PINECONE_API_KEY=your-pinecone-api-key
PINECONE_ENVIRONMENT=your-pinecone-environment

# =====================================================
# DEVELOPMENT CONFIGURATION
# =====================================================
# Only set for development - disable in production!
# DEVELOPMENT_MODE=true
