# ABSOLUTELY MINIMAL PACKAGES - Disk space critical
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
httpx==0.26.0
werkzeug==2.3.7
alembic==1.12.1
cryptography==41.0.4
email-validator==2.1.0

# Temporarily removed all large ML packages to ensure disk space:
# openai==1.3.7 (~100MB)
# chromadb==0.4.18 (~200MB with dependencies)
# numpy<2.0.0 (included with scipy/ML packages)

# Note: Basic web framework + database + auth will work
# Add back AI/ML packages after successful deployment
# Use: poetry add openai chromadb numpy scipy after deployment
